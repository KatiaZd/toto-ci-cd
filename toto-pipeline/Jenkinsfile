pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        // Jenkins checkoute déjà via SCM, mais on garde un écho pour la trace
        echo "📦 Sources récupérées depuis GitHub"
      }
    }
    stage('Hello World') {
      steps {
        echo "✅ Hello from Jenkins!"
      }
    }
  }
}
